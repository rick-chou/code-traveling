
const { defaults } = require('lodash');

/**
 * @example defaults
 * @description 
 * @param {} 
 */
const _defaults = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _defaults;
