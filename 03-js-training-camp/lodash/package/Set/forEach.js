
const { forEach } = require('lodash');

/**
 * @example forEach
 * @description 
 * @param {} 
 */
const _forEach = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _forEach;
