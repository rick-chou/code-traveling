
const { flatMapDeep } = require('lodash');

/**
 * @example flatMapDeep
 * @description 
 * @param {} 
 */
const _flatMapDeep = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _flatMapDeep;
