
const { partition } = require('lodash');

/**
 * @example partition
 * @description 
 * @param {} 
 */
const _partition = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _partition;
