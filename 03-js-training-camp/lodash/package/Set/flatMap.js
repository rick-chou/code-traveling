
const { flatMap } = require('lodash');

/**
 * @example flatMap
 * @description 
 * @param {} 
 */
const _flatMap = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _flatMap;
