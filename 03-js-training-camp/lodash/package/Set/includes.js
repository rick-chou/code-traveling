
const { includes } = require('lodash');

/**
 * @example includes
 * @description 
 * @param {} 
 */
const _includes = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _includes;
