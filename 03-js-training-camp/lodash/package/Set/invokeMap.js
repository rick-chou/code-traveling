const { invokeMap } = require('lodash');

/**
 * @example invokeMap
 * @description
 * @param {}
 */
const _invokeMap = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _invokeMap;
