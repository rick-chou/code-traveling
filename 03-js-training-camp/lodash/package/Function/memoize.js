
const { memoize } = require('lodash');

/**
 * @example memoize
 * @description 
 * @param {} 
 */
const _memoize = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _memoize;
