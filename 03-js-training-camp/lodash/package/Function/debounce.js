
const { debounce } = require('lodash');

/**
 * @example debounce
 * @description 
 * @param {} 
 */
const _debounce = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _debounce;
