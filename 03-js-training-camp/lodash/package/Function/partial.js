
const { partial } = require('lodash');

/**
 * @example partial
 * @description 
 * @param {} 
 */
const _partial = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _partial;
