
const { bindKey } = require('lodash');

/**
 * @example bindKey
 * @description 
 * @param {} 
 */
const _bindKey = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _bindKey;
