const { curry } = require('lodash');

/**
 * @name chunk(array, [size=1])
 * @param array (Array): 需要处理的数组
 * @param [size=1] (number): 每个数组区块的长度
 * @description 数组切片
 */

/**
 *
 * @param {Array} array
 * @param {number} size
 */
const _curry = (array, size = 1) => {};

if (require.main === module) {
}

module.exports = _curry;
