
const { startsWith } = require('lodash');

/**
 * @example startsWith
 * @description 
 * @param {} 
 */
const _startsWith = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _startsWith;
