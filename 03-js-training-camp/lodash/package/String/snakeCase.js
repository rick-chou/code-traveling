
const { snakeCase } = require('lodash');

/**
 * @example snakeCase
 * @description 
 * @param {} 
 */
const _snakeCase = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _snakeCase;
