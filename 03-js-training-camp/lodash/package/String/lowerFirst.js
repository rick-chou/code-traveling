const { lowerFirst } = require('lodash');

/**
 * @name clamp(number, [lower], upper)
 * @description 返回限制在 lower 和 upper 之间的值
 */

/**
 *
 * @param {number} number
 */
const _lowerFirst = (number, lower, upper) => {};

if (require.main === module) {
}

module.exports = _lowerFirst;
