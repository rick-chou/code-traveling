
const { replace } = require('lodash');

/**
 * @example replace
 * @description 
 * @param {} 
 */
const _replace = () => {
  // TODO
};

if (require.main === module) {
}

module.exports = _replace;
