/**
 * @example arr.at(index)
 * 方法接收一个整数值并返回该索引的项目，允许正数和负数。负整数从数组中的最后一个项目开始倒数
 * 匹配给定索引的数组中的元素。如果找不到指定的索引，则返回undefined
 */

console.log([1, 2, 3, 4, 5].at(-1)); // 5
console.log([1, 2, 3, 4, 5].at(1)); // 2
